{
  "name": "redis-backoff",
  "description": "Exponential backoff w/ Redis, designed for password retries",
  "version": "1.0.0",
  "author": "Jonathan Ong <me@jongleberry.com> (http://jongleberry.com)",
  "license": "MIT",
  "repository": "jonathanong/redis-backoff",
  "dependencies": {
    "native-or-bluebird": "^1.1.2"
  },
  "devDependencies": {
    "bluebird": "^2.6.0",
    "istanbul": "0",
    "mocha": "2",
    "then-redis": "^1.2.2"
  },
  "scripts": {
    "test": "mocha --reporter spec",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter dot"
  },
  "keywords": [
    "redis",
    "exponential",
    "backoff",
    "throttle",
    "throttling",
    "limit",
    "password"
  ],
  "files": [
    "index.js"
  ]
}
